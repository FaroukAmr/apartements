create .env file in root server directory and copy the following content to it, then add your credentials

PORT=8080
SECRET=secret
CLIENT_URL=http://localhost:3000 // in case you are using a web client
DB_USER=@
DB_HOST=localhost
DB_PASSWORD=@
DB_PORT=5432
DB_DATABASE_NAME=vending_machine
MONGODB_URI=@